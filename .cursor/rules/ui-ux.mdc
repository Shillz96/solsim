---
description: UI and UX quality, responsiveness, and accessibility
globs:
  - "frontend/components/**/*.tsx"
alwaysApply: false
---

### 📱 Layout
- Use grid or flex with consistent spacing (`gap-4` desktop, `gap-2` mobile).
- Support responsive breakpoints: 1-col mobile → 2-col tablet → 3-4-col desktop.
- Ensure vertical rhythm (consistent margin/padding multiples of 4 px).

### ♿ Accessibility
- Every interactive element needs `aria-label` or descriptive text.
- Maintain ≥ 4.5:1 color contrast.
- Keyboard focus must be visible and logical.
- Use semantic HTML (`<table>`, `<header>`, `<main>`).

### ⚡ Performance
- Memoize heavy components (`React.memo`).
- Use Suspense + skeletons for async lists/charts.
- Subscribe to PriceStream only for visible tokens.