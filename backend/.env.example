# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database

# Redis Configuration  
REDIS_URL=redis://localhost:6379

# Solana Configuration
SOLANA_RPC=https://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY
HELIUS_WS=wss://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY
HELIUS_API=YOUR_HELIUS_API_KEY

# External APIs
DEXSCREENER_BASE=https://api.dexscreener.com
JUPITER_BASE=https://quote-api.jup.ag
BIRDEYE_API_KEY=your-birdeye-api-key

# PumpPortal API (for real mainnet trading)
# Get API key from: https://pumpportal.fun/
# Lightning API (1% fee): Trades from deposited balance
# Local Transaction API (0.5% fee): Users sign with their wallet
PUMPPORTAL_API_KEY=your-pumpportal-api-key-here

# Security
JWT_SECRET=your-super-secure-jwt-secret-change-this-in-production
JWT_EXPIRY=24h
REFRESH_TOKEN_EXPIRY=7d

# Rewards System (OPTIONAL - for VSOL token distribution)
# Both must be configured together to enable reward claiming
# VSOL_TOKEN_MINT: Your VSOL SPL token mint address
# REWARDS_WALLET_SECRET: JSON array of secret key bytes (e.g., [1,2,3,...])
# Example: REWARDS_WALLET_SECRET=[123,45,67,89,...]
VSOL_TOKEN_MINT=
REWARDS_WALLET_SECRET=

# Real Trading Deposit System
# Platform seed for deterministic deposit address generation
# SECURITY: Keep this secret! Change in production. Min 32 characters.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PLATFORM_DEPOSIT_SEED=change-this-to-a-secure-random-seed-minimum-32-chars

# Railway Environment
NODE_ENV=production
PORT=4000

# Sentry Error Monitoring (optional)
# SENTRY_DSN=your_sentry_dsn_here
# ENABLE_SENTRY_TEST=false