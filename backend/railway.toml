[build]
builder = "nixpacks"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["openssl", "postgresql"]

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[healthcheck]
path = "/health"
port = 4002
