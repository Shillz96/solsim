# Vercel Environment Variables for SolSim Frontend
#
# Add these environment variables in your Vercel project dashboard:
# https://vercel.com/[your-team]/[project-name]/settings/environment-variables

# ==========================================
# REQUIRED PRODUCTION VARIABLES
# ==========================================

# Backend API Connection
NEXT_PUBLIC_API_URL=https://lovely-nature-production.up.railway.app
NEXT_PUBLIC_WS_URL=wss://lovely-nature-production.up.railway.app

# Application Environment
NEXT_PUBLIC_ENV=production
NEXT_PUBLIC_DEBUG=false

# ==========================================
# SOLANA WALLET INTEGRATION
# ==========================================

# Solana Network Configuration for Wallet Adapter
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
NEXT_PUBLIC_SOLANA_RPC_ENDPOINT=https://api.mainnet-beta.solana.com

# ==========================================
# DUAL-TIER SYSTEM CONFIGURATION
# ==========================================

# Display values for tier benefits (frontend only)
NEXT_PUBLIC_EMAIL_USER_SOL=10
NEXT_PUBLIC_WALLET_USER_SOL=50  
NEXT_PUBLIC_SIM_HOLDER_SOL=100

# Conversion limits for UI display
NEXT_PUBLIC_EMAIL_USER_MONTHLY_LIMIT=5
NEXT_PUBLIC_SIM_HOLDER_MONTHLY_LIMIT=50

# Minimum $SIM tokens required for premium tier
NEXT_PUBLIC_MINIMUM_SIM_TOKENS=1000

# ==========================================
# DEPLOYMENT INSTRUCTIONS
# ==========================================

# 1. Update NEXT_PUBLIC_API_URL with your Railway backend URL
# 2. Update NEXT_PUBLIC_WS_URL with your Railway backend WebSocket URL
# 3. Add all environment variables above in Vercel dashboard
# 4. Deploy frontend to Vercel
# 5. Update Railway backend FRONTEND_ORIGIN with Vercel domain

# ==========================================
# EXAMPLE URLS (Update with your actual domains)
# ==========================================

# Replace with your actual Railway backend URL:
# NEXT_PUBLIC_API_URL=https://solsim-backend-production.up.railway.app
# NEXT_PUBLIC_WS_URL=wss://solsim-backend-production.up.railway.app

# Your Vercel frontend will be:
# https://solsim.vercel.app (or your custom domain)